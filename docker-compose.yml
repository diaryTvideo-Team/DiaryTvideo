version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    container_name: diarytvideo-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-diarytvideo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-dev_password}
      POSTGRES_DB: ${POSTGRES_DB:-diarytvideo}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-diarytvideo}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
